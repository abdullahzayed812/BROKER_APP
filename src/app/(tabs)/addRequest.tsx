import { Text } from "react-native";
// import { useAppTheme } from "../_layout";

export default function AddRequestPage() {
  // const { colors } = useAppTheme();

  return <Text>AddRequestPage Page</Text>;
}
